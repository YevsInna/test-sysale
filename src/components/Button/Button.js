import React from 'react';

import '../style.css'

const Button = () => {
    return (
        <div>
            <button className={'btn-buy'}>КУПИТЬ</button>
        </div>
    );
};

export default Button;