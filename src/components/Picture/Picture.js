import React from 'react';

import '../style.css'

const Picture = () => {
    return (
        <div className={'picture'}>
            <button className={'btn-new'}>NEW</button>
            <div className={'moroccanoil'}></div>
            <div className={'icon'}></div>
        </div>

    );
};

export default Picture;